package course.java_core.basics.mini_final_project;

public class Main {
    public static void main(String[] args) {
        BookManager bookManager = new BookManager();
        bookManager.crateBookList();
        bookManager.start();
    }
}
